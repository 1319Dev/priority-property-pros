import { ComingSoonLayout } from "./ComingSoonLayout";

export function SignInPage() {
  return (
    <ComingSoonLayout
      eyebrow="Sign in"
      title="No accounts in Phase 1."
      body="There is nothing to sign into yet — no passwords, no social login, no Supabase. When accounts exist, this page will be the door."
    />
  );
}
