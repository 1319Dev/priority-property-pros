import { ComingSoonLayout } from "./ComingSoonLayout";

export function HowItWorksPage() {
  return (
    <ComingSoonLayout
      eyebrow="How it works"
      title="The marketplace in four steps."
      body="Post the project. Local independents respond. You choose. They do the work. PPP never becomes the contractor. The homepage explains each step in full — live posting is not on yet."
    />
  );
}
